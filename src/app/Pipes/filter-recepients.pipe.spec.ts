import { FilterRecepientsPipe } from './filter-recepients.pipe';

describe('FilterRecepientsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterRecepientsPipe();
    expect(pipe).toBeTruthy();
  });
});
